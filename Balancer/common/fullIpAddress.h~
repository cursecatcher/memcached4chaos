#ifndef FULLIPADDRESS_H
#define FULLIPADDRESS_H



class fullIpAddress {
	private:
		std::string ip;
		int port;
	public:
	    void setIp(string s);
	    void setPort(int  p);
	    std::string getIp();
	    int getPort();
	    MSGPACK_DEFINE(ip, port);
};

#endif
